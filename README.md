# mumblequatch.github.io

User-pages repo for Thomas Woodrow's quarterly newsletter pieces.

Each piece lives at `<yymmdd_slug>/index.html` and is served at
`https://mumblequatch.github.io/<yymmdd_slug>/`.

Pieces are generated by the `newsletter_publish` script at
`~/Scripts/newsletter_publish/`.
